<?xml version="1.0" encoding="utf-8"?>
<!--
  String resources for :service-android.

  Phase 1 only: the persistent notification posted by the foreground
  receiver service (#21). Other modules contribute their own strings
  (see :app/res/values/strings.xml for app-wide UI copy).
-->
<resources xmlns:tools="http://schemas.android.com/tools">

    <!--
      User-visible name of the notification channel that backs the
      persistent foreground notification. Shown in system Settings ->
      Notifications -> Bada.
    -->
    <string name="receiver_notification_channel_name">Quick Share receiver</string>

    <!--
      Description shown under the channel name in the system Settings
      app. Single sentence, no terminal punctuation per Material
      guidelines.
    -->
    <string name="receiver_notification_channel_description">Status notification while listening for incoming Quick Share transfers</string>

    <!--
      Persistent notification title. Kept short so the system can show
      it on the lock screen and in collapsed-shade renderings.
    -->
    <string name="receiver_notification_title">Quick Share is active</string>

    <!--
      Notification body text. Mirrors the issue body's "Receiving nearby
      files…" requirement; ellipsis preserves the original phrasing.
    -->
    <string name="receiver_notification_text">Receiving nearby files…</string>

    <!--
      Notification body variant that surfaces the current Wi-Fi SSID
      (#85). Helps the user verify both ends are on the same network
      without leaving the app. The %1$s argument is the stripped SSID
      (Android wraps SSIDs in surrounding double quotes; we strip them
      before substitution).
    -->
    <string name="receiver_notification_text_with_ssid">Receiving on \"%1$s\"</string>

    <!--
      Fallback body when the SSID is unavailable (for example on API 31+
      where WifiManager.connectionInfo can return "<unknown ssid>" for
      apps without ACCESS_FINE_LOCATION). We do not request a new
      permission for this nice-to-have surface.
    -->
    <string name="receiver_notification_text_unknown_ssid">Receiving on this network</string>

    <!--
      Consent notification (#22). Posted when an inbound connection
      reaches `WaitingForUserConsent`. Distinct channel from the
      receiver-foreground notification because it must be high-importance
      (heads-up) and audible — the user has to be unmistakably prompted.
    -->
    <string name="consent_notification_channel_name">Incoming transfer requests</string>
    <string name="consent_notification_channel_description">Heads-up prompt when another device asks to send you files</string>

    <!--
      Title shown on the consent notification. The first variant is
      preferred when we know the sender's device name; the second covers
      the hidden-visibility / parse-failure case.
    -->
    <string name="consent_notification_title_with_name">%1$s wants to share</string>
    <string name="consent_notification_title_unknown_sender">A nearby device wants to share</string>

    <!--
      Item-count summary. Pluralisation handled in code today — Phase 1
      ships English-only; `plurals` resources will land alongside the
      Korean translation pass.
    -->
    <string name="consent_notification_summary_n_items" tools:ignore="PluralsCandidate">%1$d item(s) (%2$s)</string>
    <string name="consent_notification_summary_no_items">no items</string>

    <!--
      Mixed-introduction summary (#40). When an introduction announces
      payloads of more than one kind (file + URL + address + ...), the
      consent notification renders a per-kind breakdown like "3 files +
      1 URL (12.4 MB)" instead of the opaque "4 item(s) (12.4 MB)".
      Each segment is built from one of the kind-specific strings below
      and joined with " + "; the size suffix is appended via
      consent_notification_summary_kinds_with_size.
    -->
    <string name="consent_notification_summary_kinds_with_size">%1$s (%2$s)</string>
    <string name="consent_notification_segment_files" tools:ignore="PluralsCandidate">%1$d file(s)</string>
    <string name="consent_notification_segment_urls" tools:ignore="PluralsCandidate">%1$d URL(s)</string>
    <string name="consent_notification_segment_addresses" tools:ignore="PluralsCandidate">%1$d address(es)</string>
    <string name="consent_notification_segment_phone_numbers" tools:ignore="PluralsCandidate">%1$d phone number(s)</string>
    <string name="consent_notification_segment_texts" tools:ignore="PluralsCandidate">%1$d text(s)</string>
    <string name="consent_notification_segment_photos" tools:ignore="PluralsCandidate">%1$d photo(s)</string>
    <string name="consent_notification_segment_videos" tools:ignore="PluralsCandidate">%1$d video(s)</string>
    <!-- PIN chip caption + expanded file-list strings (heads-up redesign). -->
    <string name="consent_notification_pin_label">PIN</string>
    <string name="consent_notification_filelist_item">%1$s · %2$s</string>
    <string name="consent_notification_filelist_more" tools:ignore="PluralsCandidate">…and %1$d more</string>

    <!--
      Folder-receive summary (#39). Surfaced when the introduction
      contains a `parent_folder` shared by every announced file — i.e.
      the peer is sending a folder rather than a flat list of files.
      `%1$s` is the folder name, `%2$d` the file count, `%3$s` the
      formatted total size.
    -->
    <string name="consent_notification_summary_folder" tools:ignore="PluralsCandidate">Folder \"%1$s\" (%2$d file(s), %3$s)</string>

    <!-- Action button labels. -->
    <string name="consent_notification_action_accept">Accept</string>
    <string name="consent_notification_action_reject">Reject</string>

    <!--
      Transfer progress notification (#46). Posted once the user
      accepts a transfer and the FSM enters Receiving. Distinct
      low-importance channel so progress updates do not peek over
      other UI. Updates every ~500 ms with bytes transferred,
      smoothed bytes/sec rate, and ETA.
    -->
    <string name="transfer_progress_channel_name">Transfer progress</string>
    <string name="transfer_progress_channel_description">Quiet progress notification for in-flight Quick Share transfers</string>

    <!--
      Title variants. `with_name` is preferred when the sender device
      name is known (carried over from the consent metadata);
      `unknown_sender` is used when the sender advertised in hidden
      visibility mode or when EndpointInfo.parse rejected the bytes.
    -->
    <string name="transfer_progress_title_with_name">Receiving from %1$s</string>
    <string name="transfer_progress_title_unknown_sender">Receiving files</string>

    <!--
      Body subtitle pieces. The full body is built by joining whatever
      pieces are known with " · " separators — a freshly-started
      transfer with no rate sample yet renders just the size segment;
      a steady-state transfer shows all three.
    -->
    <string name="transfer_progress_body_size">%1$s of %2$s</string>
    <string name="transfer_progress_body_rate">%1$s/s</string>
    <string name="transfer_progress_body_eta">%1$s remaining</string>

    <!--
      Duration phrasing for the ETA segment. The acceptance criterion
      calls for accessibility-friendly text ("30 seconds remaining"),
      so we spell out "seconds" / "minutes" / "hours" rather than
      using single-letter suffixes. Pluralisation handled in code
      today; `plurals` resources land with the Korean translation
      pass.
    -->
    <string name="transfer_progress_duration_few_seconds">less than a second</string>
    <string name="transfer_progress_duration_seconds" tools:ignore="PluralsCandidate">%1$d seconds</string>
    <string name="transfer_progress_duration_about_minutes" tools:ignore="PluralsCandidate">about %1$d minutes</string>
    <string name="transfer_progress_duration_about_hours" tools:ignore="PluralsCandidate">about %1$d hours</string>

    <!-- Cancel-action label on the progress notification (#46). -->
    <string name="transfer_progress_action_cancel">Cancel</string>

</resources>
